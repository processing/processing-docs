<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>/* @pjs crisp */</name>

<unsupported_modes>java</unsupported_modes>

<category>Javascript</category>
<subcategory>Directives</subcategory>

<usage>Web</usage>

<example>
<image></image>
<code><![CDATA[
/* @pjs crisp="true"; */
void setup()
{
  size(200,200);
  noLoop();
}

void draw()
{
  background(255);
  line(width/4, height/2, width/2, height/4); 
  line(width/2, height/4, 3*width/4, height/2); 
  line(3*width/4, height/2, width/2, 3*height/4);
  line(width/2, 3*height/4, width/4, height/2);
  rect(width/2 - 25, height/2 - 25, 50, 50);
}
]]></code>
</example>

<description><![CDATA[
<p>Depending on the browser's default settings, canvas elements may or may not apply anti-aliassing to draw instructions. This directive lets 
you force plain (not anti-aliassed) rendering of the "line", "triangle", and "rect" draw primitives. This results in crisp clean lines, 
although angled lines may look jagged.</p>
<p><b>NOTE:</b> the "arc", "ellipse" and "bezier" draw primitives are not effected by this directive.</p>
<p><b>NOTE:</b> this directive is very expensive, and will result in a significant drop in framerate, and increase in CPU use. </p>
]]></description>

<syntax>
<![CDATA[       
/* @pjs crisp="true"; */
]]></syntax>

<returns></returns>

<parameter>
	<label>crisp</label>
	<description>"true" (implicitly false for any other value)</description>
</parameter>

<related>
directives
</related>

<availability>2.0</availability>

<type>Operator</type>

<partof>PDE</partof>

</root>

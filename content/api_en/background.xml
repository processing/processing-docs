<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>background()</name>

<category>Color</category>

<subcategory>Setting</subcategory>

<type></type>

<example>
<image>background_0.png</image>
<code><![CDATA[
background(51);
]]></code>
</example>

<example>
<image>background_1.png</image>
<code><![CDATA[
background(255, 204, 0);
]]></code>
</example>

<example>
<image>background_2.png</image>
<code><![CDATA[
PImage img;
img = loadImage("laDefense.jpg");
background(img);
]]></code>
</example>

<description><![CDATA[
The <b>background()</b> function sets the color used for the background of the Processing window. The default background is light gray.  <b>background()</b> is typically used within the <b>draw()</b> function to clear the display window at the beginning of each frame.
<br/> <br/>
An image can also be used as the background for a sketch, although the image's width and height must match that of the sketch window. To resize an image to the size of the sketch window, use image.resize(width, height).
<br/> <br/>
Images used with <b>background()</b> will ignore the current <b>tint()</b> setting.
<br/> <br/>
It is not possible to use transparency (alpha) with background colors on the main drawing surface; you can acheive the same effect with <b>createGraphics()</b>.
]]></description>

</root>

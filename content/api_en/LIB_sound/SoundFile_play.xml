<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>play()</name>

<category>Sound Files</category>

<subcategory></subcategory>

<usage>Web &amp; Application</usage>

<example>
<image></image>
<code><![CDATA[
import processing.sound.*;
SoundFile file;

void setup() {
  size(640, 360);
  background(255);
    
  // Load a soundfile from the /data folder of the sketch and play it back
  file = new SoundFile(this, "sample.mp3");
  file.play();
}

void draw() {
}
]]></code>
</example>

<description><![CDATA[
Starts the playback of the soundfile. Only plays to the end of the soundfile once. If cue() or pause() were called previously, playback will resume from the cued position.
]]></description>

</root>

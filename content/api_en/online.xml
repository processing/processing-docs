<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>online</name>

<category>Environment</category>

<subcategory></subcategory>

<type>variable</type>

<example>
<image></image>
<code><![CDATA[
if (online) {  // Or "if (online == true)"
  // Draw a circle
  ellipse(50, 50, 60, 60);
} else {
  // Draw an X
  line(0, 0, 100, 100);
  line(100, 0, 0, 100);
}
]]></code>
</example>

<description><![CDATA[
Reports whether or not a Processing program is running as a Java applet inside of a web browser. The <b>online</b> variable is "true" if running as a web applet and "false" if not.
]]></description>

</root>

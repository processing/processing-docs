<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>loadShader()</name>

<unsupported_modes>js</unsupported_modes>

<category>Rendering</category>

<subcategory>Shaders</subcategory>

<type></type>

<example>
<image></image>
<code><![CDATA[
PShader blur;

void setup() {
  size(640, 360, P2D);
  // Shaders files must be in the "data" folder to load correctly
  blur = loadShader("blur.glsl"); 
  stroke(0, 102, 153);
  rectMode(CENTER);
}

void draw() {
  filter(blur);  
  rect(mouseX-75, mouseY, 150, 150); 
  ellipse(mouseX+75, mouseY, 150, 150);
}
]]></code>
</example>

<description><![CDATA[
Loads a shader into the PShader object. The shader file must be loaded in the sketch's "data" folder to load correctly. Shaders are compatible with the P2D and P3D renderers, but not with the default renderer.
]]></description>

</root>

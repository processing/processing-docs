<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>

<name>setString()</name>

<category>JSONObject</category>

<subcategory>Method</subcategory>

<type>method</type>

<example>
<image></image>
<code><![CDATA[
JSONObject json;

void setup() {

  json = new JSONObject();
  
  json.setInt("count", 88);
  json.setFloat("weight", 35.432);
  json.setString("name", "celery");
  json.setBoolean("isFruit", false);

  int count = json.getInt("count");
  float weight = json.getFloat("weight");
  String name = json.getString("name");
  boolean isFruit = json.getBoolean("isFruit");

  println(count + ", " + weight + ", " + name + ", " + isFruit);
}

// Sketch prints:
// 88, 35.432, celery, false
]]></code>
</example>

<description><![CDATA[
Inserts a new key/String pair into the <b>JSONObject</b> or, if a value with the specified key already exists, assigns a new value.
]]></description>

</root>

<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>ellipseMode()</name>

<category>Shape</category>

<subcategory>Attributes</subcategory>

<example>
<image>ellipseMode_.png</image>
<code><![CDATA[
ellipseMode(CENTER);
ellipse(35, 35, 50, 50);
ellipseMode(CORNER);
fill(102);
ellipse(35, 35, 50, 50);
]]></code>
</example>

<description><![CDATA[
The origin of the ellipse is modified by the <b>ellipseMode()</b> function. The default configuration is <b>ellipseMode(CENTER)</b>, which specifies the location of the ellipse as the center of the shape. The <b>RADIUS</b> mode is the same, but the width and height parameters to <b>ellipse()</b> specify the radius of the ellipse, rather than the diameter. The <b>CORNER</b> mode draws the shape from the upper-left corner of its bounding box. The <b>CORNERS</b> mode uses the four parameters to <b>ellipse()</b> to set two opposing corners of the ellipse's bounding box. The parameter must be written in ALL CAPS because Processing is a case-sensitive language.
]]></description>

</root>

<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>hex()</name>

<category>Data</category>

<subcategory>Conversion</subcategory>

<type></type>

<example>
<image></image>
<code><![CDATA[
color c = #ffcc00;
println(c);          // Prints "-13312"
println(hex(c));     // Prints "FFFFCC00"
println(hex(c, 6));  // Prints "FFCC00"
]]></code>
</example>

<example>
<image></image>
<code><![CDATA[
color c = color(255, 204, 0);
println(c);          // Prints "-13312"
println(hex(c));     // Prints "FFFFCC00"
println(hex(c, 6));  // Prints "FFCC00"
]]></code>
</example>


<description><![CDATA[
Converts an <b>int</b>, <b>byte</b>, <b>char</b>, or <b>color</b> to a <b>String</b> containing the equivalent hexadecimal notation. For example, the <b>color</b> value produced by <b>color(0, 102, 153)</b> will convert to the <b>String</b> value <b>"FF006699"</b>. This function can help make your geeky debugging sessions much happier.<br />
<br />
Note that the maximum number of digits is 8, because an <b>int</b> value can only represent up to 32 bits. Specifying more than 8 digits will not increase the length of the <b>String</b> further.
]]></description>

</root>

<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>getVertex()</name>

<category>PShape</category>

<subcategory>Method</subcategory>

<type></type>

<example>
<image></image>
<code><![CDATA[
PShape s;

void setup() {
  size(100, 100, P2D);
  s = createShape();
  s.vertex(0, 0);
  s.vertex(60, 0);
  s.vertex(60, 60);
  s.vertex(0, 60);
  s.end(CLOSE);
}

void draw() {
  translate(20, 20);
  for (int i = 0; i < s.getVertexCount(); i++) {
    PVector v = s.getVertex(i);
    v.x += random(-1, 1);
    v.y += random(-1, 1);
    s.setVertex(i, v);
  }
  shape(s);
}
]]></code>
</example>

<description><![CDATA[
The <b>getVertex()</b> method returns a PVector that has the coordinates of the vertex point located at the position defined by the <b>index</b> parameter. 
]]></description>

</root>

<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>disableStyle()</name>

<category>PShape</category>

<subcategory>Method</subcategory>

<type></type>

<example>
<image>PShape_disableStyle_.png</image>
<code><![CDATA[
PShape s;

void setup() {
  // The file "bot.svg" must be in the data folder
  // of the current sketch to load successfully
  s = loadShape("bot.svg");
}

void draw() {
  s.disableStyle();
  shape(s, -30, 10, 80, 80);
  s.enableStyle();
  shape(s, 50, 10, 80, 80);
}
]]></code>
</example>

<description><![CDATA[
Disables the shape's style data and uses Processing's current styles. Styles include attributes such as colors, stroke weight, and stroke joints. 
]]></description>

</root>

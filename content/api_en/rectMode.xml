<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>rectMode()</name>

<category>Shape</category>

<subcategory>Attributes</subcategory>

<type></type>

<example>
<image>rectMode_.png</image>
<code><![CDATA[
rectMode(CENTER);
fill(255);
rect(35, 35, 50, 50);  // White rect

rectMode(CORNER);
fill(102);
rect(35, 35, 50, 50);  // Gray rect
]]></code>
</example>

<description><![CDATA[
Modifies the location from which rectangles draw. The default mode is <b>rectMode(CORNER)</b>, which specifies the location to be the upper left corner of the shape and uses the third and fourth parameters of <b>rect()</b> to specify the width and height. The syntax <b>rectMode(CORNERS)</b> uses the first and second parameters of <b>rect()</b> to set the location of one corner and uses the third and fourth parameters to set the opposite corner. The syntax <b>rectMode(CENTER)</b> draws the shape from its center point and uses the third and forth parameters of <b>rect()</b> to specify the shape's width and height. The syntax <b>rectMode(RADIUS)</b> draws the image from its center point and uses the third and forth parameters of <b>rect()</b> to specify half of the shapes's width and height.<br />
<br />
The parameter must be written in ALL CAPS because Processing is a case sensitive language.
]]></description>

</root>
